# PhotoFlow runtime dependencies.
#
# MediaPipe and Streamlit are part of PhotoFlow's overall tech stack but are
# NOT installed yet — they're added to this file as each milestone introduces
# the code that depends on them (remaining Milestone 2 analysis: MediaPipe;
# Milestone 3: Streamlit).
#
# Pillow and ImageHash are required by core/duplicate_detector.py for image
# loading and perceptual hashing. OpenCV and NumPy are required by
# core/blur_detector.py for Variance-of-Laplacian blur scoring. The headless
# OpenCV build is used because PhotoFlow needs no GUI windowing.

PyYAML>=6.0,<7.0
Pillow>=10.0,<13.0
ImageHash>=4.3,<5.0
opencv-python-headless>=4.8,<5.0
numpy>=1.24,<3.0
mediapipe>=0.10,<0.11
