[pytest]
testpaths = tests
python_files = test_*.py
filterwarnings =
    # Starlette deprecated its own 422 constant; the warning comes from
    # FastAPI's built-in validation handler, not from our code.
    ignore:.*HTTP_422_UNPROCESSABLE_ENTITY.*:DeprecationWarning
