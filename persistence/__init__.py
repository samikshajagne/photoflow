"""
Result persistence and caching.

Intentionally empty in Milestone 1 (Setup). Starting in Milestone 2/3
this package will hold the SQLite-backed cache (cache_db.py) that keys
per-image analysis results by (path, size, mtime) to support resumable
and incremental runs.
"""
